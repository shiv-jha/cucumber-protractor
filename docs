
cd protractor-cucumber-example
Install Dependencies--  npm install
Start Webdriver Manager (in 1 terminal window)  --- npm run start-webdriver
Run Protractor (in another terminal window)--  npm run protractor
